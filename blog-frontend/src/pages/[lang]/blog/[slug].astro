---
import BaseLayout from "../../../layouts/BaseLayout.astro";
import RelatedPosts from "../../../components/RelatedPosts.astro";
import { fetchPosts, fetchPost } from "../../../lib/blog";

export async function getStaticPaths() {
  const [postsEs, postsEn] = await Promise.all([fetchPosts("es"), fetchPosts("en")]);

  return [
    ...postsEs.map((p) => ({ params: { lang: "es", slug: p.uid } })),
    ...postsEn.map((p) => ({ params: { lang: "en", slug: p.uid } })),
  ];
}

const { lang, slug } = Astro.params as { lang: "es" | "en"; slug: string };

const post = await fetchPost(lang, slug);
if (!post) {
  return Astro.redirect(`/${lang}/blog`);
}

// ── Language Switcher: apunta al slug del post traducido, si existe ───────────
const oppositeLang = lang === "es" ? "en" : "es";
const switchUrl = post?.alternateSlug
  ? `/${oppositeLang}/blog/${post.alternateSlug}`
  : `/${oppositeLang}/blog`;

// ── Posts del mismo idioma para RelatedPosts ──────────────────────────────────
const allPosts = await fetchPosts(lang);

const publishedDate = post?.publishedAt
  ? new Date(post.publishedAt).toLocaleDateString(lang === "es" ? "es-MX" : "en-US")
  : "";
---

<BaseLayout
  title={`${post.metaTitle || post.title} | ZOOBLOG`}
  description={post.metaDescription || post.description}
  ogImage={post.image || undefined}
  switchUrl={switchUrl}
>
  <section class="bg-gradient-to-br from-emerald-50 via-white to-sky-50">
    <div class="mx-auto max-w-6xl px-6 py-20">

      <!-- HERO DEL ARTÍCULO -->
      <div class="mx-auto max-w-4xl text-center">
        <div class="flex justify-center flex-wrap gap-3">
          {post.category && (
            <a
              href={`/${lang}/categoria/${post.category.slug}`}
              class="rounded-full bg-emerald-600 px-4 py-2 text-sm font-black text-white hover:bg-emerald-700"
            >
              {post.category.name}
            </a>
          )}
          {post.tags.map((tag) => (
            <a
              href={`/${lang}/etiqueta/${tag.slug}`}
              class="rounded-full bg-emerald-100 px-4 py-2 text-sm font-black text-emerald-700 hover:bg-emerald-200"
            >
              #{tag.name}
            </a>
          ))}
        </div>

        <p class="mt-8 text-sm font-black uppercase tracking-[0.25em] text-slate-500">
          {publishedDate}
        </p>

        <h1 class="mt-6 text-5xl font-black leading-tight tracking-tight text-slate-950 md:text-7xl">
          {post.title}
        </h1>

        <p class="mx-auto mt-8 max-w-3xl text-xl leading-9 text-slate-600">
          {post.description}
        </p>
      </div>

      <!-- IMAGEN PRINCIPAL -->
      {post.image && (
        <div class="mx-auto mt-16 max-w-5xl overflow-hidden rounded-[3rem] bg-slate-200 shadow-2xl shadow-emerald-100">
          <img
            src={post.image}
            alt={post.imageAlt}
            class="h-[560px] w-full object-cover"
          />
        </div>
      )}

    </div>
  </section>

  <!-- CONTENIDO -->
  <section class="bg-white">
    <div class="mx-auto grid max-w-6xl gap-12 px-6 py-24 lg:grid-cols-[220px_1fr]">

      <!-- SIDEBAR INTERNO -->
      <aside class="hidden lg:block">
        <div class="sticky top-28 rounded-[2rem] border border-emerald-100 bg-emerald-50 p-6">
          <p class="text-xs font-black uppercase tracking-[0.25em] text-emerald-700">
            ZOOBLOG
          </p>
          <p class="mt-4 text-sm leading-6 text-slate-600">
            {lang === "es"
              ? "Artículo educativo sobre el mundo animal."
              : "Educational article about the animal world."}
          </p>
        </div>
      </aside>

      <!-- ARTÍCULO -->
      <article class="article-content" set:html={post.content} />

    </div>
  </section>

  <RelatedPosts currentPost={post} lang={lang} allPosts={allPosts} />
</BaseLayout>
